#include <stdio.h>
#include "myfunc.h"
#include <math.h>

int Func(double a, double b, double c)
{
    values value;
    double x1, x2;
    double Diskr;

    Diskr=b*b-(4*a*c);
    if(Diskr > 0 )
    {
	x1=(-1*b+sqrt(Diskr))/2*a;
	x2=(-1*b-sqrt(Diskr))/2*a;
    }
    
}